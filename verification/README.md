# Verification

Bootstrap verification starts with the C++ architectural model and its
deterministic GoogleTest suite. RTL lint is provided through Verilator. UVM,
DPI-C, and processor integration are planned layers, not CI requirements yet.